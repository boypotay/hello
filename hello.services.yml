services:
  hello.currentuser:
    class: Drupal\hello\Service\HelloCurrentuser
    
  hello.route_subscriber:
    class: Drupal\hello\Routing\RouteSubscriber
    tags:
      - { name: event_subscriber }